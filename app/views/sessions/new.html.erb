<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:page_title)) %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application" %>
    <%= javascript_importmap_tags %>
    <%= Sentry.get_trace_propagation_meta.html_safe %>
  </head>

  <body class="h-screen w-full">
    <main class="h-full w-full sm:flex">
      <div class="flex items-center justify-center min-h-screen bg-gray-100">
        <div class="w-full max-w-md p-6 bg-white rounded-lg shadow-md">
          <h1 class="text-2xl font-bold text-center text-gray-800 mb-6">ログイン</h1>

          <%= form_with url: signin_path, method: :post, local: true, class: "space-y-4" do |form| %>
            <div>
              <%= form.label :display_name, "表示名", class: "block text-sm font-medium text-gray-700" %>
              <%= form.text_field :display_name, class: "w-full mt-1 p-2 border border-gray-300 rounded-md shadow-sm focus:ring-indigo-500 focus:border-indigo-500", placeholder: "表示名を入力" %>
            </div>

            <div>
              <%= form.label :password, "パスワード", class: "block text-sm font-medium text-gray-700" %>
              <%= form.password_field :password, class: "w-full mt-1 p-2 border border-gray-300 rounded-md shadow-sm focus:ring-indigo-500 focus:border-indigo-500", placeholder: "パスワードを入力" %>
            </div>

            <div>
              <%= form.submit "ログイン", class: "py-2 px-4 bg-indigo-600 font-medium rounded-md hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
            </div>
          <% end %>
        </div>
      </div>
    </main>
  </body>
</html>

<%= form_with model: book do |f| %>
  <% if book.errors.any? %>
    <div>
      <%= book.errors.full_messages.to_sentence.capitalize %>
    </div>
  <% end %>

  <%= f.text_field :title %>
  <%= f.select :category, Book.categories.keys %>
  <%= f.submit %>
<% end %>
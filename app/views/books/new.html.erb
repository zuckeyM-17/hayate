x<% content_for(:page_title) { 'New Book' } %>


<%= render 'shared/breadcrumb', parents: [{url: '/books', title: 'Books'}] %>
<div class="p-4">
  <%= render 'shared/header', title: 'New Book' %>

  <%= render "form", book: @book %>
</div>
